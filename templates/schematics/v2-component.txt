import { V2Container, V2Text, V2Separator } from '@spraxium/components';

interface {{pascalName}}Data {
  title: string;
  content: string;
}

@V2Container({ accentColor: 0x5865f2 })
export class {{pascalName}}Component {
  @V2Text((data: {{pascalName}}Data) => `## ${data.title}`)
  header!: string;

  @V2Separator()
  divider!: never;

  @V2Text((data: {{pascalName}}Data) => data.content)
  body!: string;
}
