import { SlashCommand } from '@spraxium/common';

@SlashCommand({
  name: 'restyle',
  description: 'Demo: render-time overrides for static buttons & selects.',
})
export class RestyleCommand {
  build() {}
}
