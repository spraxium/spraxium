import { SlashCommand } from '@spraxium/common';

@SlashCommand({
  name: 'quickactions',
  description: 'Demo: V2 @V2DynamicRow (components mode) + select inside V2.',
})
export class QuickActionsCommand {
  build() {}
}
