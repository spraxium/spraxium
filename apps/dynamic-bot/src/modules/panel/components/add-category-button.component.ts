import { Button } from '@spraxium/components';

/** Shown inside the empty-state block when a panel has no categories. */
@Button({ customId: 'panel-add', label: 'Add Category', style: 'success', emoji: '➕' })
export class AddCategoryButton {}
