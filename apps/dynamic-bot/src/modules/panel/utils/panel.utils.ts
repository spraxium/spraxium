/** Builds the standard ticket-opened confirmation message. */
export function ticketOpenedMessage(categoryName: string): string {
  return `🎫 Ticket opened for **${categoryName}**. Our team will be in touch shortly!`;
}
