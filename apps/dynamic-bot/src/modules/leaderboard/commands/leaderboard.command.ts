import { SlashCommand } from '@spraxium/common';

@SlashCommand({
  name: 'leaderboard',
  description: 'Demo: V2 container with @V2DynamicRow (dynamic + items mode).',
})
export class LeaderboardCommand {
  build() {}
}
