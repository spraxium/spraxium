import { SlashCommand } from '@spraxium/common';

@SlashCommand({
  name: 'ui-demo',
  description: 'Demo dos novos componentes V2 — checkbox group e radio group.',
})
export class UiDemoCommand {
  build() {}
}
