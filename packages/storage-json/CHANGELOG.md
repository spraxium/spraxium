# @spraxium/storage-json

## Unreleased

### Minor Changes

- Add native Zod schemas to document and collection definitions, including parsed-value normalization and
  detailed validation causes. Custom type guards remain supported for backwards compatibility.

## 1.0.0

### Major Changes

- Add typed JSON document and collection stores with atomic writes, runtime validation, decorators, and Spraxium lifecycle integration.
