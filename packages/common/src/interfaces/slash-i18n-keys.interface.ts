export interface SlashI18nKeys {
  /** Locale lookup key for this entity's name localizations. */
  name: string;
  /** Locale lookup key for this entity's description localizations. */
  description: string;
}
